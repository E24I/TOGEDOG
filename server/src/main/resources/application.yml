spring:
  profiles:
    default: dev # ?? ???? dev / ??? prod


#### ?? ?? ?? ####
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${EMAIL_SENDER}
    password: ${EMAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000
    auth-code-expiration-millis: 1800000

cloud:
  aws:
    credentials:
      secret-key: AKIA55PP54W7UO5EICHDsrNYuxm
      access-key: FV5pGsqli0iuOf2IDWIN4cRxFu6aSA0A
    s3:
      bucket: togedog-image
    region:
      static: ap-northeast-2
    stack:
      auto: false

logging:
  level:
    com:
      amazonaws:
        util:
          EC2MetadataUtils: error
